<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>
    <style name="NoTitle" parent="Theme.AppCompat.Light.NoActionBar">

    <item name="android:activityOpenEnterAnimation">@android:anim/slide_in_left</item>
    <item name="android:activityOpenExitAnimation">@android:anim/slide_out_right</item>
    <item name="android:activityCloseEnterAnimation">@android:anim/slide_in_left</item>
    <item name="android:activityCloseExitAnimation">@android:anim/slide_out_right</item>

</style>
    <style name="SplashTheme" parent="NoTitle">
        <item name="android:windowBackground">@drawable/start</item>
    </style>

    <style name="mainTheme" parent="NoTitle">
        <item name="android:windowBackground">@drawable/main</item>
    </style>
    <style name="detailTheme" parent="NoTitle">
        <item name="android:windowBackground">@drawable/base_background</item>
    </style>
    <style name="passwordTheme" parent="NoTitle">
        <item name="android:windowBackground">@drawable/background</item>
    </style>
    <style name="connectTheme" parent="NoTitle">
        <item name="android:windowBackground">@drawable/bluetooth_connect</item>
    </style>

    <style name="mydatepick" >

        <item name="android:theme">@android:anim/slide_in_left</item>
        <item name="android:activityOpenExitAnimation">@android:anim/slide_out_right</item>
        <item name="android:activityCloseEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:activityCloseExitAnimation">@android:anim/slide_out_right</item>

    </style>

</resources>
